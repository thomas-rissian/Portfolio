---
import Layout from '../layouts/Layout.astro';

const projets = await Astro.glob('../content/numerique-responsable/*.md');
---

<Layout pageTitle="Numérique Responsable">
  <div class="projects-container">
    <div class="projects-grid">
      {projets.map(projet => (
        <a href={`/numerique-responsable/${projet.frontmatter.slug}/`} class="project-card">
          <h2 class="project-card-title">{projet.frontmatter.titre}</h2>
          <div class="card-arrow">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </div>
        </a>
      ))}
    </div>
  </div>
</Layout>